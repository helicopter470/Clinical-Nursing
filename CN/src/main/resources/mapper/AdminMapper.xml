<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.AdminMapper">

    <select id="selectAll" resultType="com.example.entity.Admin">
        select * from admin
        <where>
            <if test="name!=null">and name like concat('%',#{name},'%')</if>
        </where>
    </select>

    <insert id="insert" parameterType="com.example.entity.Admin" useGeneratedKeys="true">
        insert into admin(id,username,password,avatar,name,phone,role)
        values (#{id},#{username},#{password},#{avatar},#{name},#{phone},#{role})
    </insert>

    <update id="updateById" parameterType="com.example.entity.Admin">
        update admin set username=#{username},password=#{password},avatar=#{avatar},
                         name=#{name},phone=#{phone},role=#{role}
        where id=#{id}
    </update>
</mapper>
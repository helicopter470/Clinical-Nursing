<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.PatientMapper">

    <select id="selectAll" resultType="com.example.entity.Patient">
        select patient.* , nurse.name AS nurseName
        FROM patient
        LEFT JOIN nurse ON patient.nurse_id = nurse.id
        <where>
            <if test="name !=null">and patient.name like concat('%',#{name},'%')</if>
        </where>
    </select>

    <insert id="insert" parameterType="com.example.entity.Patient" useGeneratedKeys="true">
        insert into patient(id,username,password,avatar,name,phone,
                            sex,age,idcard,address,money,role,nurse_id)
        values (#{id},#{username},#{password},#{avatar},#{name},#{phone},
               #{sex},#{age},#{idcard},#{address},#{money},#{role},#{nurseId})
    </insert>

    <update id="updateById" parameterType="com.example.entity.Patient">
        update patient set username=#{username},password=#{password},avatar=#{avatar},
                           name=#{name},phone=#{phone},sex=#{sex},age=#{age},
                           idcard=#{idcard},address=#{address},money=#{money},role=#{role},
                           nurse_id=#{nurseId}
        where id=#{id}
    </update>
</mapper>